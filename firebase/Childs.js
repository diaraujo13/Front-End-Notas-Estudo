// Com o método push estaremos criando novos 'child' no link a qual
// fazemos referência
ref.push(object)

// Para acessar um 'child' específico, utilizamos o .child(item_key)
// pois todos 'child' possuem um id ao serem criados (por exemplo, criados via push)
// - Com referncia ao child podemos passar o método .update()
